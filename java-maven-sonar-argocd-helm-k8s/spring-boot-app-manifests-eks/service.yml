apiVersion: v1
kind: Service
metadata:
  name: spring-boot-app-service
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: LoadBalancer
  selector:
    app: spring-boot-app